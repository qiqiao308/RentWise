host: localhost
port: 6379
db: 0
decode_responses: true
# RAG摘要结果缓存TTL(秒)
rag_summarize_ttl: 3600
# Agent回复缓存TTL(秒)
agent_reply_ttl: 1800
# 对话历史TTL(秒)
chat_history_ttl: 86400
# 每个session最大对话消息数
chat_history_max_len: 50
